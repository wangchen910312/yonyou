<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chn.bhmc.dms.sal.veh.service.dao.VehicleSpecOptionDAO">

	<!-- 모델 목록  -->
	<select id="selectVehicleOptionByCondition" parameterType="VehicleSearchVO" resultType="VehicleVO">
        SELECT * FROM (
            SELECT ROWNUM AS RNUM, T1.* FROM (
                SELECT OPTION_CD,OPTION_NM,OPTION_EN_NM
                      ,START_DT,END_DT,REG_USR_ID,REG_DT
                  FROM SA_0103T
                 WHERE 1=1
                 <if test='option != null and option != ""'>
                    <choose>
                        <when test='@org.apache.commons.lang3.StringUtils@contains(option,"*")'>
                            <bind name='option' value='@org.apache.commons.lang3.StringUtils@replace(option, "*", "%")' />
                             AND OPTION_NM LIKE #{option}
                        </when>
                        <otherwise>
                             AND OPTION_NM = #{option}
                        </otherwise>
                    </choose>
                </if>
                <if test='startDt != null and endDt != null'>
                   <![CDATA[
                   AND END_DT >= #{startDt}
                   AND #{endDt} >= START_DT
                   ]]>
                </if>
            ) T1
            <where>
                <if test='firstIndex != -1 and lastIndex != -1'>
                    ROWNUM <![CDATA[<=]]> #{lastIndex}
                </if>
            </where>
        )
        <where>
            <if test='firstIndex != -1 and lastIndex != -1'>
                RNUM > #{firstIndex}
            </if>
        </where>
         ORDER BY
         <choose>
            <when test='sort != null'>
                <foreach item="item" index="index" collection="sort" open="" separator="," close="">
                    <choose>
                        <when test='item.field == "optionCd"'>OPTION_CD <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "optionNm"'>OPTION_NM <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "optionEnNm"'>OPTION_EN_NM <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "startDt"'>START_DT <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "endDt"'>END_DT <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "regUsrId"'>REG_USR_ID <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <when test='item.field == "regDt"'>REG_DT <choose><when test='item.dir == "asc"'>ASC</when><otherwise>DESC</otherwise></choose></when>
                        <otherwise>
                            OPTION_CD ASC
                        </otherwise>
                    </choose>
                </foreach>
            </when>
            <otherwise>
                OPTION_CD ASC
            </otherwise>
        </choose>
	</select>

	<!-- 모델 목록 갯수  -->
	<select id="selectVehicleOptionByConditionCnt" parameterType="VehicleSearchVO" resultType="int">
        SELECT COUNT(*) AS VALUE
          FROM SA_0103T
         WHERE 1=1
         <if test='optionCd != null and optionCd != ""'>
           AND OPTION_CD = #{optionCd}
         </if>
         <if test='startDt != null and endDt != null'>
           <![CDATA[
           AND END_DT >= #{startDt}
           AND #{endDt} >= START_DT
           ]]>
         </if>
	</select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    SQL File Name : RoleIfDAO_SqlMap.xml
    Description : 역할 전송 배치 DAO
    author Kwon Ki hyun
    since 2016. 8. 26.
    version 1.0
    Modification Information
       since          author              description
    ===========    =============    ===========================
    2016. 08. 26.  kwon kihyun     최초 생성
-->


<mapper namespace="chn.bhmc.dms.bat.cmm.ath.service.dao.RoleIfDAO">
    <!-- 역할정보 테이블[CM_0804T]에서 역할정보 송신 테이블[CM_0804IS]에 반영한다. -->
    <update id="insertRoleIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0804T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>

    <!-- 역할계층정보 테이블[CM_0805T]에서 역할계층정보 송신 테이블[CM_0805IS]에 반영한다. -->
    <update id="insertRoleHierarchyIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0805T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>

    <!-- 역할-직무매핑정보 테이블[CM_0803T]에서 역할-직무매핑정보 송신 테이블[CM_0803IS]에 반영한다. -->
    <update id="insertRoleAuthoritiesIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0803T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>

    <!-- 화면계층정보 테이블[CM_0808T]에서 화면정보 송신 테이블[CM_0808IS]에 반영한다. -->
    <update id="insertViewHierarchyIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0808T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>

    <!-- 화면정보 테이블[CM_0809T]에서 화면정보 송신 테이블[CM_0809IS]에 반영한다. -->
    <update id="insertViewInfoIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0809T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>

    <!-- 화면정보-역할매핑정보 테이블[CM_0810T]에서 화면정보-역할매핑정보 송신 테이블[CM_0810IS]에 반영한다. -->
    <update id="insertViewAuthoritiesIf" parameterType="int" statementType="CALLABLE" >
         {call SP_COM_DCS_DMS_CM_0810T(
            #{value, mode=IN, jdbcType=NUMERIC}
         )}
    </update>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    SQL File Name : GdmsDataMigDAO_SqlMap.xml
    Description : 배치작업에 관한 데이터처리
    author Kim Yunseung
    since 2017. 7. 22.
    version 1.0
    Modification Information
       since          author              description
    ===========    =============    ===========================
    2017. 07. 22.  Kim Yunseung     최초 생성
-->


<mapper namespace="chn.bhmc.dms.bat.ser.mig.service.dao.GdmsDataMigDAO">

    <!-- GDMS 기초정보를 가져온다.-->
    <select id="insertBaseInfoGdmsToDcs" statementType="CALLABLE" >
         { call MIG.SP_BASE_INFO_GDMS_TO_DCS_MIG('')  }
    </select>

    <!-- GDMS PWA를 가져온다.-->
    <select id="insertPwaGdmsToDcs" statementType="CALLABLE" >
         { call MIG.SP_PWA_GDMS_TO_DCS_MIG('')  }
    </select>

    <!-- GDMS 클레임을 가져온다.-->
    <select id="insertClaimGdmsToDcs" statementType="CALLABLE" >
         { call MIG.SP_CLAIM_GDMS_TO_DCS_MIG('')  }
    </select>

    <!-- GDMS RO를 가져온다.-->
    <select id="insertRoGdmsToDcs" statementType="CALLABLE" >
         { call MIG.SP_RO_GDMS_TO_DCS_MIG('')  }
    </select>

</mapper>